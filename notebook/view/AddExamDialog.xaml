<Window x:Class="notebook.AddExamDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Créer un examen" Height="350" Width="450" WindowStartupLocation="CenterScreen">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Module -->
        <TextBlock Grid.Row="0" Grid.Column="0" Text="Module :" VerticalAlignment="Center" Margin="5"/>
        <ComboBox x:Name="ModuleBox" Grid.Row="0" Grid.Column="1" DisplayMemberPath="Name" Margin="5"/>

        <!-- Date -->
        <TextBlock Grid.Row="1" Grid.Column="0" Text="Date :" VerticalAlignment="Center" Margin="5"/>
        <DatePicker x:Name="DatePicker" Grid.Row="1" Grid.Column="1" Margin="5"/>

        <!-- Absent -->
        <TextBlock Grid.Row="2" Grid.Column="0" Text="Absent :" VerticalAlignment="Center" Margin="5"/>
        <CheckBox x:Name="AbsentBox" Grid.Row="2" Grid.Column="1" Margin="5"/>

        <!-- Teacher -->
        <TextBlock Grid.Row="3" Grid.Column="0" Text="Enseignant :" VerticalAlignment="Center" Margin="5"/>
        <TextBox x:Name="TeacherBox" Grid.Row="3" Grid.Column="1" Margin="5"/>

        <!-- Note -->
        <TextBlock Grid.Row="4" Grid.Column="0" Text="Note :" VerticalAlignment="Center" Margin="5"/>
        <TextBox x:Name="NoteBox" Grid.Row="4" Grid.Column="1" Margin="5"/>

        <!-- Coefficient -->
        <TextBlock Grid.Row="5" Grid.Column="0" Text="Coefficient :" VerticalAlignment="Center" Margin="5"/>
        <TextBox x:Name="CoefBox" Grid.Row="5" Grid.Column="1" Margin="5"/>

        <!-- Buttons -->
        <StackPanel Grid.Row="6" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">
            <Button Content="Valider" Width="100" Margin="10" Click="Confirm"/>
            <Button Content="Annuler" Width="100" Margin="10" Click="Cancel"/>
        </StackPanel>
    </Grid>
</Window>
